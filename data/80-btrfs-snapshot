DPkg::Pre-Invoke {"if [ -x /usr/bin/apt-btrfs-snapshot ] && apt-btrfs-snapshot supported > /dev/null; then apt-btrfs-snapshot snapshot; else echo System configuration does not support apt-btrfs-snapshot. ; fi "; };
